apiVersion: v1
kind: Service
metadata:
  name: {{ include "cogvlm.fullname" . }}
  labels:
    {{- include "cogvlm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cogvlm.selectorLabels" . | nindent 4 }}
