apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cogvlm.fullname" . }}-test-connection"
  labels:
    {{- include "cogvlm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cogvlm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
